name: .NET Core Desktop

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

  # You may pin to the exact commit or the version.
  # uses: yashmahi88/build-01@c81a5bd74495d223081162a1fd7ba2dfa071ae53
    uses: yashmahi88/build-01@v2
    with:
      dotnet-version: 10.0
          
