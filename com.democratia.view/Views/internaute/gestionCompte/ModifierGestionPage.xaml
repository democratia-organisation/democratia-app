<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="com.democratia.Views.internaute.gestionCompte.ModifierGestionPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:component="clr-namespace:com.democratia.Views.Component"
    xmlns:lang="clr-namespace:com.democratia.view.Resources.Localization"
    xmlns:viewModel="clr-namespace:com.democratia.ViewModels.internaute.gestionCompte;assembly=com.democratia.core"
    Title="ModifierGestionPage"
    x:DataType="viewModel:ModifierGestionViewModel"
    Style="{DynamicResource fondEcran}">

    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <component:Header Grid.Row="0" />
            <StackLayout
                x:Name="stackLayout"
                Grid.Row="1"
                Padding="10">
                <BoxView HeightRequest="{StaticResource SpacingLarge}" />
                <component:EntryComponent
                    x:Name="nomDeFamilleComponent"
                    Title="{x:Static lang:AppResources.Nom}"
                    ValeurDonne="{Binding TempInternaute.nom_internaute}" />
                <component:EntryComponent
                    x:Name="prenomComponent"
                    Title="{x:Static lang:AppResources.prenom}"
                    ValeurDonne="{Binding TempInternaute.prenom_internaute}" />
                <component:EntryComponent
                    x:Name="addresseComponent"
                    Title="{x:Static lang:AppResources.adress}"
                    ValeurDonne="{Binding TempInternaute.adresse_postale}" />
                <component:EntryComponent
                    x:Name="mailComponent"
                    Title="{x:Static lang:AppResources.mail}"
                    ValeurDonne="{Binding Email}" />
                <component:EntryComponent
                    x:Name="passwordComponent"
                    Title="{x:Static lang:AppResources.Mdp}"
                    PassWord="True"
                    ValeurDonne="{Binding Password}" />
                <Label
                    x:Name="retourMessageLabel"
                    AutomationId="RetourMessage"
                    HorizontalOptions="Center"
                    Style="{DynamicResource ErrorLabelStyle}"
                    Text="{Binding RetourMessage}" />
                <BoxView HeightRequest="100" />
                <Button
                    x:Name="inscriptionButton"
                    AutomationId="M'inscrire"
                    AutomationProperties.Name="M'inscrire"
                    Command="{Binding ModifierInternauteCommand}"
                    Style="{DynamicResource ButtonStyle}"
                    Text="{x:Static lang:AppResources.enregistrer}" />
            </StackLayout>
        </Grid>
    </ScrollView>
</ContentPage>