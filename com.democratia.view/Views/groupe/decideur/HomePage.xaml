<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="com.democratia.Views.groupe.decideur.HomePage"
             xmlns:viewModel="clr-namespace:com.democratia.ViewModels.groupe;assembly=com.democratia.core"
             xmlns:viewModelDecideur="clr-namespace:com.democratia.ViewModels.groupe.decideur;assembly=com.democratia.core"
             Title="HomePage"
             x:DataType="viewModelDecideur:HomeViewModel">
    <VerticalStackLayout>
        <!--TODO : mettre le reste de la partie décideur-->
        <ScrollView
            Scrolled="ScrollView_Scrolled">
            <VerticalStackLayout
                x:DataType="viewModel:GroupeViewModel"
                BindableLayout.ItemsSource="{Binding Propositions}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <!--TODO : faire le composant proposition avec les fonctionnalités décideur-->
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>
        </ScrollView>
    </VerticalStackLayout>
</ContentPage>